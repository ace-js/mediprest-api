const admin = require('./admin')
const auth = require('./auth')
const billing = require('./billing')
const delegateOrAdmin = require('./delegateOrAdmin')

module.exports = {
  admin,
  auth,
  billing,
  delegateOrAdmin
}
